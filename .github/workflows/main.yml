name: longor

on:
  push:
    branches:
      - main
      - staging

jobs:
  runner-1:
    runs-on: ubuntu-latest
    env:
      BEAM_TOKEN: ${{ secrets.BEAM_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Beam
        run: |
          python -m pip install --upgrade pip
          python -m pip install "beam-client==0.2.207"

      - name: Configure Beam
        run: |
          beam configure default --token "$BEAM_TOKEN"

      - name: Deploy
        run: |
          beam deploy \
            --name monthly-expense-1 \
            --cpu 4 \
            mui.py:momo


  runner-2:
    runs-on: ubuntu-latest
    env:
      BEAM_TOKEN: ${{ secrets.BEAM_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Beam
        run: |
          python -m pip install --upgrade pip
          python -m pip install "beam-client==0.2.207"

      - name: Configure Beam
        run: |
          beam configure default --token "$BEAM_TOKEN"

      - name: Deploy
        run: |
          beam deploy \
            --name monthly-expense-2 \
            --cpu 4 \
            mui.py:momo


  runner-3:
    runs-on: ubuntu-latest
    env:
      BEAM_TOKEN: ${{ secrets.BEAM_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Beam
        run: |
          python -m pip install --upgrade pip
          python -m pip install "beam-client==0.2.207"

      - name: Configure Beam
        run: |
          beam configure default --token "$BEAM_TOKEN"

      - name: Deploy
        run: |
          beam deploy \
            --name monthly-expense-3 \
            --cpu 4 \
            mui.py:momo


  runner-4:
    runs-on: ubuntu-latest
    env:
      BEAM_TOKEN: ${{ secrets.BEAM_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Beam
        run: |
          python -m pip install --upgrade pip
          python -m pip install "beam-client==0.2.207"

      - name: Configure Beam
        run: |
          beam configure default --token "$BEAM_TOKEN"

      - name: Deploy
        run: |
          beam deploy \
            --name monthly-expense-4 \
            --cpu 4 \
            mui.py:momo


  runner-5:
    runs-on: ubuntu-latest
    env:
      BEAM_TOKEN: ${{ secrets.BEAM_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Beam
        run: |
          python -m pip install --upgrade pip
          python -m pip install "beam-client==0.2.207"

      - name: Configure Beam
        run: |
          beam configure default --token "$BEAM_TOKEN"

      - name: Deploy
        run: |
          beam deploy \
            --name monthly-expense-5 \
            --cpu 4 \
            mui.py:momo
